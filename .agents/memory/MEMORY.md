- [Express 5 params typing](express5-params.md) — `req.params.x` is `string | string[]` in @types/express-serve-static-core@5.x; always cast with `as string`
- [Bot state module](bot-state.md) — bot-state.ts holds singleton client ref + getBotStatus(); index.ts calls setBotClient(); ClientReady also calls setBotClient() to reset uptime to ready time; /api/status is public (no auth)
- [New DB tables 2026-06](new-db-tables.md) — warnings, automod_configs, giveaways, ticket_modules, antiraid_whitelist, auto_roles, guild_webhooks, ticket_queue, custom_commands added; all pushed to DB
- [Bot interaction handlers](bot-interactions.md) — ticket buttons use custom_id pattern ticket_close/claim/delete/reopen_{id}; auto-roles use autorole_{id}; select menu ticket_select_module_{panelId}; panel buttons ticket_panel_{id}; custom slash commands handled via isChatInputCommand() BEFORE button check
- [Native canvas build](canvas-build.md) — @napi-rs/canvas must be externalized in build.mjs; add "@napi-rs/canvas", "@napi-rs/canvas-linux-x64-gnu", "@napi-rs/canvas-linux-x64-musl" to external array; dynamic import inside try/catch in welcome-card.ts
- [Verification system patterns](verification-system.md) — send-panel uses Discord LINK button (style 5); role assign via Bot PUT /guilds/:id/members/:uid/roles/:rid; verify-info endpoint now auto-creates config row if missing (no more 404)
- [Smart selectors pattern](smart-selectors.md) — GuildChannelSelect + GuildRoleSelect components fetch from /api/guilds/:guildId/channels|roles; fall back to text Input if empty; types[] filter for channel type (0=text,5=announce,2=voice,4=category)
- [Bot customization helper](bot-customization.md) — sendToChannelCustomized() in bot.ts checks guild webhookRow by (guildId, channelId) before falling back to sendToChannel(); only triggers on pro/ultra + webhookBotName||webhookBotAvatar
- [Ticket system patterns](ticket-system-patterns.md) — safeDefer uses ephemeral:true (not flags:64); welcome embed ALWAYS sent (not conditional on welcomeEmbedEnabled); queue: queueEnabled+maxConcurrentTickets in ticketConfigsTable; processNextInQueue auto-creates channel via guild.channels.create without interaction
- [Custom commands route](custom-commands-route.md) — registers/unregisters Discord guild slash commands via REST (POST /applications/{appId}/guilds/{guildId}/commands) in real-time on create/update/delete; bot handles isChatInputCommand before button check
- [Layout component usage](layout-component.md) — pages must use Layout (not DashboardLayout which doesn't exist); always pass guildId prop: `<Layout guildId={guildId}>`
- [Anti-DDoS patterns](anti-ddos-patterns.md) — parallel banning via Promise.allSettled; setVerificationLevel(level 4) + setSlowmodeOnAllChannels(30s) on raid; new-account velocity tracker (8+ new accs <30d in 30s); invite spam tracker (5+ invites/60s); express-rate-limit on API (300/min global, 30/15min auth)
- [502 fix pattern](502-fix.md) — "Start application" workflow must kill stale port before Vite: `fuser -k 5000/tcp 2>/dev/null; sleep 0.5;` prefixed to the command via configureWorkflow()
