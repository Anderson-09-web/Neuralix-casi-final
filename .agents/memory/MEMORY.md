- [Express 5 params typing](express5-params.md) — `req.params.x` is `string | string[]` in @types/express-serve-static-core@5.x; always cast with `as string`
- [Bot state module](bot-state.md) — bot-state.ts holds singleton client ref + getBotStatus(); index.ts calls setBotClient(); ClientReady also calls setBotClient() to reset uptime to ready time; /api/status is public (no auth)
- [New DB tables 2026-06](new-db-tables.md) — warnings, automod_configs, giveaways, ticket_modules, antiraid_whitelist, auto_roles, guild_webhooks added; pushed to DB; see routes: auto-roles, webhooks, messages, whitelist, ticket modules
- [Bot interaction handlers](bot-interactions.md) — ticket buttons use custom_id pattern ticket_close/claim/delete/reopen_{id}; auto-roles use autorole_{id}; select menu ticket_select_module; all defer+edit reply; GuildVoiceStates intent added for voice logs
